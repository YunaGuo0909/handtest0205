[project]
name = "handtest0205"
version = "0.2.0"
description = "实时手部追踪与手势识别 - MediaPipe + UDP传输到UE"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mediapipe>=0.10.0",
    "opencv-python>=4.8.0",
    "numpy>=1.24.0",
    "ultralytics>=8.0.0",
]
